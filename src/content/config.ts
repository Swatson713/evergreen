// src/content/config.ts
//
// All content is managed through Keystatic (keystatic.config.ts).
// Data is read at build time using the @keystatic/core/reader API —
// see src/pages/index.astro and src/pages/projects/[slug].astro.
//
// This file is kept intentionally minimal to avoid conflicts.

export const collections = {};
